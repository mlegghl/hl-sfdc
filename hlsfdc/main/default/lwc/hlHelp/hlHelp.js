import { LightningElement } from 'lwc';
import HL_GIF from '@salesforce/resourceUrl/rvalow';

export default class HlContacts extends LightningElement {
    hlGifUrl = HL_GIF;
    helpResourceUrl = 'https://helplightning.com/support/';
}